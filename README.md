# Vue Vuetify Firefox Extension

Nuova ![favicon.ico](icons/favicon.ico).

In Firefox, go to `about:debugging` and install it as local extension. You will find it in the browser bar near other extensions.